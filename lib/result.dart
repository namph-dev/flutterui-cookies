import 'package:flutter/material.dart';

class ResultPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(color: Color(0xFFFFFBEF),
     child: Column(children: [
      
     ],)
    );
  }
}
